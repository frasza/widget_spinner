name: widget_spinner
description: Widget spinner package can be used to rotate desired widget on tap - inspired by fidget spinner! Number of spins and duration of the animation can be modified.
version: 0.0.3
homepage: https://github.com/frasza/widget_spinner

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
